# Operator source

`org/us_ignite/thingworx/operator/` contains the Java operator. `test/` contains fast unit tests plus the dependencies needed for future cluster integration tests. Build with `./gradlew :apps:thingworx-operator:test`.
