# Custom-resource API

`ThingWorxCluster` is the stable boundary between Kubernetes users and reconciliation code. Add HA settings only after documenting their validation, defaults, immutability, and upgrade behavior. Secrets must be referenced by name, never represented as plaintext fields.
